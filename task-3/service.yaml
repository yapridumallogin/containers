apiVersion: v1
kind: Service
metadata:
  name: nginx-service
  namespace: dz5
spec:
  type: NodePort
  selector:
    app: nginx
  ports:
    - port: 80
      targetPort: 80
      nodePort: 30080  # диапазон NodePort по умолчанию: 30000-32767